# --- App URL (Auth.js / OAuth redirects) ---
AUTH_URL=http://localhost:3000

# --- Auth.js ---
# Generate: `npx auth secret` (or `openssl rand -base64 32`)
AUTH_SECRET=

# --- Postgres (Neon or Vercel Postgres) ---
DATABASE_URL=

# --- Dexcom OAuth ---
DEXCOM_CLIENT_ID=
DEXCOM_CLIENT_SECRET=

# --- Strava OAuth ---
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=

# --- Patterns (Anthropic) ---
ANTHROPIC_API_KEY=

# --- Apple Shortcuts → hourly steps ingest ---
# Shared secret sent as Authorization: Bearer … or X-Shortcut-Secret (match app code)
STEPS_INGEST_SECRET=

# --- Vercel Cron (protect /api/cron/* routes) ---
CRON_SECRET=

# --- Optional: encrypt OAuth refresh tokens at rest ---
TOKEN_ENCRYPTION_KEY=
